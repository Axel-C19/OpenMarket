#![no_std]
use gstd::{wait, ActorId,};
use gcore::exec;

pub struct ack {
    cliente: Option<bool>,
    vendedor: bool,
}

pub struc 

impl ack_contract {
    fn handle_wallets(wallet:ActorId, usuario: &str) {
        if usuario == "vendedor" {
            self.vendedor = true
        }
        else if usuario == "cliente" {
            self.cliente = true
        }
    }
}

#[no_mangle]
unsafe extern "C" fn init(){
    
}

#[no_mangle]
unsafe extern "C" fn handle(){
    
}

